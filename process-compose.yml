version: '0.5'

processes:
  npm-prepare:
    command: npm install

  vite:
    command: npm run dev
    availability:
      restart: "always"
    ports:
      - 5173:5173

    depends_on:
      npm-prepare:
        condition: process_completed_successfully
